extends:
  - ../../.eslintrc.yaml

parser: '@typescript-eslint/parser'
parserOptions:
  project: ./tsconfig.json
  ecmaVersion: 2020

env:
  node: true

overrides:
  - files: [ './index.ts' ]
    rules:
      import/extensions: off
