import { id, version } from '../package.json';

export const APP_ID = id;
export const APP_VERSION = version;
export const SERVICE_ID = `${APP_ID}.service`;
export const PIP_APP_ID = `${APP_ID}.camera`;
